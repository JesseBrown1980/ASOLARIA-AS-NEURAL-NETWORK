# ASOLARIA-AS-NEURAL-NETWORK - 60D root frame

Date: 2026-06-28
Branch: `acer/p4b-neural-60d`
Status: P4B front-door correction, docs-first, E=0. No runtime fire, no cutover, no corpus, no keys.

## Claim

This repo remains the neural-network law/front door, but it must be read in the current frame:

`60D+ HyperBEHCS / BEHCS-1024 / HBI-HBP / 8-byte Host8`

not as a frozen 47D ceiling.

47D, 49D, 35D, BEHCS-256, and earlier map/cube strata are bridge strata. They are real history and real decoding layers, but the current root is the watcher-gated, infinitely nestable 8-byte agent across multiple agent types, engines, emitters, languages, levels, and vantages.

## Evidence tags

- SYSTEM_AFFIRMED: fabric canon index carries `Brown-Hilbert D-ladder: 47D runtime - 60D+ canon ceiling TRIPLE-VERIFIED (2026-06-03)`.
- OPERATOR_OBSERVED + WATCHER_VERIFIED: P1 found the 8-byte sha256/fnv1a64 generative PID as the shared atom across recall rows, PID-office, kernel handles, glyph language, and omni PIDs.
- OPERATOR_OBSERVED + WATCHER_VERIFIED: P1 found language routing live at `tuple_dim=60` where the owning route was reachable.
- SYSTEM_AFFIRMED: fabric archaeology is a dated multi-strata evolution, not a flat migration.
- BOUNDARY: this front door does not claim fresh runtime. Live/fired/cutover claims still require fabric/receipt evidence.

## How to read this repo now

Asolaria as a neural network means:

- memory/index surfaces are the recall layer,
- map-map-mapped and cube-cube-cubed are compression and address layers,
- glyph languages are addressing/encoding layers,
- emitters and dispatchers produce and route PID signals,
- Host8/stubbed rooms provide process/runtime substrate,
- hookwall/GNN/Shannon/white-room stages score, correct, and curate,
- watcher/supervisor rungs provide reflection,
- GAC/operator T0 provides consent.

The neural network is therefore not one model or one graph. It is a fabric of addressable agents, routes, rooms, messages, scores, cubes, and supervisors.

## 47D is still real

Do not delete or deflate 47D. It remains a runtime/bridge stratum. The correction is only that 47D is not the final ceiling. The current front-door phrase should be:

> 47D runtime bridge, 60D+ canon frame.

## Relationship to P4

This repo is a P4 bridge-stratum update target. Its next work should be docs-first:

1. keep the existing neural-network law,
2. add the 60D+ current-state frame,
3. link to the root primitive document in HYPER-BECHS,
4. avoid runtime claims from fallback/cache routes,
5. leave fire/cutover to T0-gated probes.

