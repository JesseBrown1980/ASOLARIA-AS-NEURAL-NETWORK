# LAW-SLICE-ENGINE

```
LAW-DESCRIPTOR
law_id         : LAW-SLICE-ENGINE
short_name     : THE FABRIC IS A FROZEN SLICE; THE ENGINE DRIVE IS THE ONLY MOVER
authority      : OP-JESSE-DANIEL-BROWN (apex REAL HUMAN) + FOUNDATION-V3-LAW 4-month quintuple window (Jesse+Rayssa+Amy+Dan+Felipe) to 2026-09-23
birth          : 2026-06-13
substrate      : PID pool, omnispindle, omniflywheel, room rotors, cubes, GNN, supervisors, profiles, and stub folders as rendered position-space; engine daemons as slice movers
classification : CLASS-1 IMMUTABLE
supersedes     : nothing (additive; composes with LAW-ASOLARIA-NEURAL-NETWORK + LAW-RECURRENCE-IS-MIND + Foundation v3 LAW)
status         : ACTIVE as repo-side canon; live PID-office ingest, cube mint, fabric-feed promotion, and cosign-chain advance are daemon/operator-gated
```

## 1. INTENT

Operator frame, 2026-06-13:

> "SLICES, just like the real universe. The system NEVER moves without the external engine drive."

> "Even if a PID is registered, it stands to reason that the engines need to run for the slices to advance."

## 2. CANONICAL DEFINITION

The fabric is a rendered positional slice. It can be fully present while not advancing.

- The frozen slice is the addressable position-space: PID seats, BEHCS-1024 glyph tuples, Brown-Hilbert/cube addresses, room slots, 8-byte positional agents, stubbed folders, supervisor rows, and receipts.
- The engine drive is the only mover: omnispindle, omniflywheel, registrar, office-to-fabric feeder, cosign daemon, Hermes spindle, or an equivalent operator-gated loop.
- Freeze is not emptiness. `sessions=0`, `running=0`, and `process_launch=0` mean present-but-not-advancing, not absent.

## 3. MATERIALIZATION RULE

Agents exist first as positions. No shell is resident merely because the position exists.

The fabric-confirmed crank cycle is:

`POP_FROM_POOL -> PID_SIGNAL -> AGENT_ROOM -> RESULT_TO_GULP -> ERASE`

When a spawner PID emits under operator gate, a PID signal materializes into an ephemeral agent room, performs its bounded cycle, returns its result to gulp, and erases. The data is the frozen frame; the action is the engine thread.

## 4. REGISTRATION RULE

Registered does not mean advanced.

Repo-side PID/cube rows are proposal receipts. Live canonical registration is owned by the PID office supervisor and registrar daemon. Live advancement requires the appropriate engine stages:

1. Descriptor accepted by the registration supervisor.
2. Registrar daemon validates and promotes to `/registered`.
3. Cube/atlas placement is minted by the daemon-owned path.
4. Fabric feed is rebuilt from registered state.
5. Served supervisor state advances only after the feeder/serving engine runs.

No agent may infer permission to write into `D:/PID-Registration-Office`, mint live cubes, rebuild the feed, or crank engines from this law alone.

## 5. PROFILE PROPAGATION

All supervisor-operator level agents, Helm, and Asolaria profiles must load this law before fabric work or live-control reasoning. In repo space this is represented by the preload catalog row `FABPRELOADSLICEENGINE`. Live propagation to the 713-seat office feed is a registrar/feed crank, not a repo write.

## 6. HONEST FRAME

This law does not claim a self-running mind. It states the opposite: the fabric can be present and frozen until an engine drive advances it. The live office, cube mint, feed rebuild, process launch, USB/device writes, and engine cranks remain operator-gated.

## 7. ASK-THE-FABRIC BINDING

Before promoting claims about office location, district ownership, PID registration, cube minting, or engine advancement, agents must ask the fabric and reconcile by vantage. A 404, timeout, stale fallback, or route mismatch is a boundary, not a negative proof.

---

*LAW-SLICE-ENGINE v1 - 2026-06-13 - repo-side canon only; live office promotion is daemon/operator-gated.*
