# Asolaria-as-Neural-Network — Layered Census (NN slice), 2026-06-18

Built under the **ANTI-DEFLATION COUNT LAW** (canon in the reductions repo: `what-is-asolaria---how-do-we-get-reductions-in-everything/canon/laws/ANTI-DEFLATION-COUNT-LAW.md`). Source: census workflow `wd710opm4` (9 agents). **No single total — answer by layer, each tagged MEASURED / CANON / MODEL / UNVERIFIED.** Live = only `E≠0`.

The NN: **neurons** = frozen Gemma-4-4B slice + borrowed free-subscription LLM slices; **connections** = GNN edges (WIRED, never re-trained); **forward pass** = the 9-stage omniflywheel; **learning signal** = self-reflect + corrective gate (recurrence + ground-truth gate = mind).

## NN-relevant layers

- **L3 supervisor seats (roster = 726):** NN-class prof seats — PROF-HRM-BRIDGE, PROF-GNN-EDGELEVEL, PROF-SHANNON, PROF-DASEIN (self-reflect), PROF-SENTINEL (anomaly witness). MEASURED.
- **L4 hierarchy — self-reflect / heal tiers:** self-reflect = PROF-DASEIN (observe w/o act) + PROF-SENTINEL + `asolaria-self-reflect-daemon` (PID 5640); heal = 9 watchdog seats (cpu/gpu/memory/network/process/substrate/hookwall/cosign-chain/rook) + node-watchdog (PID 5960). CANON/MEASURED-daemon.
- **Model-reasoning neuron-seats (CANON):** Fischer-AoT · MLC · **MTP (0 roster rows — kernel-CANON, in registration lane; absence ≠ absent)** · HRM (PROF-HRM-BRIDGE) · geospatial/space-agent · **Gemma-4-4B frozen-slice** · hyper-hermes · hermes (24 HERMES-SPINDLE-PID-00..23 + 74 helpers) · PI · Agent-0. **AoT = 0 explicit rows (naming gap, may be embedded under hrm-slow-fast / GNN edges).**
- **L5 logical agents:** 5 prime-tiers × 16 levels × 60D; the 3-organ path = organ-1 answer-producer (`score_stub=-1.0`) → organ-2 Fischer scorer (fills score + reverseGain) → organ-3 supervisor-minter (GATED). City-model: sessions=0 until summoned.

## GLM-5.2 in the NN

GLM-5.2 is **entirely absent** from this repo (`grep -riw glm` = 0). Its first NN address is the Fischer-lane descriptor (acer commit `d3b09eb` / `6cb98d6`, model-selector role `glm-genius-slice`, `status=GATED`) — candidate live backer for organ-2 `score_kind=FISCHER_LIVE`, replacing the draft-standin **only under operator gate** (catch C036).

## Agent-type file types (operator note, layer-2 refinement)

The registry carries **per-agent-type file types** — claude, bigpickle, codex, **and many other agent types** each have their own file class. A census must NOT collapse across agent types: each is a distinct neuron class. (This is one reason waves "get lost" — they read one agent-type's files as the whole.)

## Principles + honest boundary

No-node / no-JSON **hot path** (HBP/HBI/binary/hash/hex/sha256/glyph hot; JSON = cold-compat only); each neuron a **Rust 8-byte host stubbed-folder**. Nothing fired (E=0): GLM call gated, organ-2 `:4794` + organ-3 mint gated, GNN heal lanes `:4792/:4793` down this session. Capacity is never reported as live.
