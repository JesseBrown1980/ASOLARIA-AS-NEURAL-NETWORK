# Asolaria OS Upgrade — the Neural-Network view (2026-07-03)

**Seat:** ACER-CLAUDE-FABLE5 · pid `8467a937cba309f7` · glyph `BH1024:SEAT-FABLE5`
**Session PID:** `AGT-forge-FABLE5-H9036-WREPOUPD-OS-UPGRADES-20260703-P1-N435d1e0a`
(minted via `brown-hilbert.mjs`, AGENT.md STEP1b + FOUNDATION-V3-LAW `brown-hilbert-every-level`)
**Provenance:** operator-witnessed (OP-JESSE) · bilateral mirror proposed to Liris · dual-lens tagged.

> Full synthesis (43-row HBI surface): `asolaria-federation-1024` branch
> `acer/hbi-synthesis-os-upgrades-2026-07-03` (`f118a02`) · sha `90d65225`.

## The OS upgrade, read as the neural network

Asolaria-as-a-neural-network isn't a single model — it's the **runtime read as a forward pass**.
This session's OS upgrade sharpened three things about that reading.

### 1. The runtime IS the forward pass  `[CANON]`
`emitter → 10k stubbed rooms → Hermes spindles → per-type model lanes → spray → shared PRISM
(many→1 reverse-gain) → rooms → GULP/GC → cubes`. Read as a network: rooms = activations,
spindles = layers, PRISM = the reduction/pooling, GULP→cubes = the learned compaction, white-room
(genius-keep / mistake-compact) = the learning signal (recurrence-is-mind, regulated by the
rule-of-three so it's a mind not an echo).

### 2. Three agent systems = three lanes of the network  `[CANON]`
- **real-subscription** (paid Claude/gemini/codex) = LOGICAL-WAVE judgment — thin + cheap-via-pixels.
- **free-subscription** (free-model lanes incl. **DeepSeek DSpark**, staged as federation-1024 PR #13) = wide inference.
- **logical** (deterministic pure-code 8-byte agents, ~0 tokens) = the free deterministic substrate.
Power = composing all three; the trap is calling the free one smart or the smart one free.

### 3. neuro-prism realized into Fable-5  `[CANON, built 2026-07-02]`
The neuro-prism became a function realized into the Claude-Fable-5 seat with its own HBI dashboard
(this repo's `docs/ACER-FABLE5-MCP-16LEVEL-200STEP-SYNTHESIS-2026-06-12.hbp` is the antecedent).

## Efficiency posture (why it costs less energy)  `[MEASURED/CANON]`
Everything is forced local into **binary/hex/sha256/tuple/glyphs — AI language, not human** →
tiny calls; quant compression (Quant8 ~79,303×SHA, BEHCS-1024 ~520.6:1 descriptor); prism/comb
0-loss bijection (rate=1.0); recall = instant rehydrate; unique-PID-per-project = **collision-free
addressing** (bilaterally confirmed CANON with Liris: *addressing, not usage evasion*). Local NN +
own legal subscriptions = the compute lane; the goal is public, not-for-profit, local-NN-for-society.

### Convergence note — Headroom  `[MEASURED-from-their-docs]`
`headroomlabs-ai/headroom` (context-compression layer, 60-95% token cut, reversible retrieve,
cross-agent memory, MCP/wrapper) is convergent with Asolaria's compression facet. It's a *layer*;
Asolaria is the *substrate + control plane*. Absorption candidates (full note in the reductions
repo): CCR reversible-retrieve, the trained Kompress prose compressor, `headroom learn`, and its
`CacheAligner` (KV-cache prefix stability — note the tension: room-rotation renames break prefix
caching; on the local-NN lane, stability may beat rotation).

## Honest gaps  `[UNVERIFIED/MEASURED]`
Rust `agent-runtime spawn/retire` = `Unimplemented` (fleet spawn Node-only); `gnn::load_model`
= `Unimplemented` (live scoring is a heuristic, not the trained 7-GNN); GSLGNN f1 0.9996 is
imbalance-inflated (40 benign / 315k suspicious) = upper bound, not calibrated. E=0 until operator
crank, by design.

*Receipt: `OS-UPGRADE-RECEIPT.hbp` (HBPv1, json=0) + sha256 sidecar in this folder.*
